# Permite ordenar los registros resultantes de una consulta en orden ascendente o descendente.

# Por ejemplo, se desea ver el título y duración de los capítulos almacenados en la base de datos:

SELECT titulo, duracion FROM episodios

# Sin embargo, salen en desorden, y se quieren ver la ordenados de forma ascendente (Menor a mayor), partiendo de la duración. Por defecto, ORDER BY los ordena de forma ascendente.

SELECT titulo, duracion FROM episodios ORDER BY duración

# Por defecto, ORDER BY los ordena de forma ascendente. Si se quiere ordenar la duración de forma descendente (Mayor a menor):

SELECT titulo, duracion FROM episodios ORDER BY duracion DESC
